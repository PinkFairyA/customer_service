import React from 'react';
import { useSelector } from 'react-redux';

class CustomerAppDateTime extends React.Component {
  render() {
    const { appointmentDate, appointmentTime } = this.props;
    return (
      <div></div>
    );
  }
}